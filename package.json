{
  "name": "usora",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm run format:check && npm run lint && npm test && npm run validate",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "oxlint --deny-warnings . && node --check scripts/usora-mcp.mjs && node --check scripts/usora-mcp.test.mjs && node --check scripts/validate-plugin.mjs && node --check commitlint.config.cjs",
    "prepare": "husky",
    "test": "node --test scripts/usora-mcp.test.mjs",
    "validate": "node scripts/validate-plugin.mjs"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "husky": "^9.1.7",
    "oxlint": "^1.78.0",
    "prettier": "3.6.2",
    "prettier-plugin-jsdoc": "^1.8.1",
    "prettier-plugin-packagejson": "^3.0.2"
  }
}
